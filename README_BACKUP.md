# 🔒 نظام النسخ الاحتياطي الشامل

نظام متكامل للنسخ الاحتياطي واستعادة المشروع بأكمله.

---

## 📁 الملفات

### 1. **backup-system.ps1** - النظام الأساسي
السكريبت الرئيسي للنسخ الاحتياطي مع خيارات متقدمة.

#### الاستخدام:
```powershell
# نسخة احتياطية كاملة مضغوطة
.\backup-system.ps1

# نسخة بدون ضغط
.\backup-system.ps1 -Compress:$false

# نسخة مع node_modules
.\backup-system.ps1 -IncludeNodeModules

# نسخة بدون تنظيف تلقائي
.\backup-system.ps1 -AutoClean:$false

# تغيير مدة الاحتفاظ (60 يوم بدلاً من 30)
.\backup-system.ps1 -KeepDays 60

# نسخة لوجهة مخصصة
.\backup-system.ps1 -Destination "E:\MyBackups"
```

#### المميزات:
✅ استثناء تلقائي للمجلدات غير الضرورية (node_modules, .git, dist, etc.)
✅ ضغط تلقائي للنسخة
✅ حذف النسخ القديمة تلقائياً
✅ سجل تفصيلي لكل عملية
✅ حساب حجم النسخة ونسبة الضغط
✅ معلومات مفصلة عن كل نسخة

---

### 2. **backup-restore.ps1** - استعادة النسخ
استعادة أي نسخة احتياطية سابقة.

#### الاستخدام:
```powershell
# عرض القائمة واختيار النسخة
.\backup-restore.ps1

# استعادة لمسار مخصص
.\backup-restore.ps1 -RestorePath "D:\joker\workspace\test"

# استعادة فورية بدون تأكيد
.\backup-restore.ps1 -Force
```

#### المميزات:
✅ عرض جميع النسخ المتاحة
✅ معلومات تفصيلية عن كل نسخة (التاريخ، الحجم)
✅ فك ضغط تلقائي للنسخ المضغوطة
✅ شريط تقدم للعملية
✅ تحذيرات قبل الاستبدال

---

### 3. **backup-auto.ps1** - النسخ التلقائي
تنفيذ نسخ احتياطي تلقائي بشكل دوري.

#### الاستخدام:
```powershell
# نسخة تلقائية كل 6 ساعات (افتراضي)
.\backup-auto.ps1

# نسخة تلقائية كل 3 ساعات
.\backup-auto.ps1 -IntervalHours 3

# نسخة واحدة فقط
.\backup-auto.ps1 -RunOnce
```

#### المميزات:
✅ نسخ احتياطي دوري تلقائي
✅ يعمل في الخلفية
✅ عرض وقت النسخة التالية
✅ إمكانية الإيقاف بـ Ctrl+C

---

### 4. **backup-quick.bat** - نسخة سريعة
اختصار سريع لعمل نسخة احتياطية بنقرة واحدة.

#### الاستخدام:
```
انقر نقراً مزدوجاً على الملف
```

---

### 5. **backup-restore.bat** - استعادة سريعة
اختصار سريع لاستعادة نسخة احتياطية.

#### الاستخدام:
```
انقر نقراً مزدوجاً على الملف
```

---

## 📊 هيكل النسخة الاحتياطية

```
D:\joker\backups\
├── backup_20251203_140530\          # نسخة غير مضغوطة
│   ├── backup-info.json              # معلومات النسخة
│   ├── src\                          # كود المصدر
│   ├── public\                       # الملفات العامة
│   ├── package.json
│   └── ...
│
├── backup_20251203_150245.zip        # نسخة مضغوطة
│
└── backup-log.txt                    # سجل جميع العمليات
```

---

## 🎯 السيناريوهات الشائعة

### 🔹 نسخة احتياطية يومية
```powershell
# في Task Scheduler أو Cron
.\backup-system.ps1 -Compress -AutoClean -KeepDays 7
```

### 🔹 نسخة قبل تحديث مهم
```powershell
.\backup-system.ps1 -Compress -AutoClean:$false
```

### 🔹 نسخة كاملة مع node_modules
```powershell
.\backup-system.ps1 -IncludeNodeModules -Compress
```

### 🔹 استعادة نسخة معينة
```powershell
.\backup-restore.ps1
# ثم اختر الرقم من القائمة
```

---

## ⚙️ الإعدادات الافتراضية

| الإعداد | القيمة الافتراضية | الوصف |
|--------|-------------------|-------|
| BackupType | full | نوع النسخة (full/incremental/differential) |
| Destination | D:\joker\backups | مجلد النسخ الاحتياطية |
| Compress | true | ضغط النسخة |
| IncludeNodeModules | false | تضمين node_modules |
| AutoClean | true | حذف النسخ القديمة تلقائياً |
| KeepDays | 30 | عدد أيام الاحتفاظ بالنسخ |

---

## 📋 المجلدات المستثناة تلقائياً

- `node_modules` (يمكن تضمينه بـ -IncludeNodeModules)
- `.git`
- `dist`
- `build`
- `.vscode`
- `.idea`
- `coverage`
- `.cache`
- `temp`
- `tmp`

---

## 🔔 التنبيهات والتحذيرات

### ⚠️ مساحة القرص
تأكد من وجود مساحة كافية في قرص النسخ الاحتياطية:
- نسخة غير مضغوطة: ~500 MB
- نسخة مضغوطة: ~100-200 MB

### ⚠️ استعادة النسخة
استعادة النسخة ستستبدل جميع الملفات الحالية!
تأكد من عمل نسخة احتياطية قبل الاستعادة.

### ⚠️ node_modules
لا يتم نسخ node_modules افتراضياً.
بعد الاستعادة قد تحتاج لتشغيل:
```bash
npm install
```

---

## 📈 إحصائيات

يوفر النظام إحصائيات تفصيلية:
- ✅ عدد الملفات المنسوخة
- ✅ حجم النسخة قبل وبعد الضغط
- ✅ نسبة الضغط
- ✅ الوقت المستغرق
- ✅ عدد النسخ الإجمالية
- ✅ المساحة الكلية للنسخ

---

## 🛡️ الأمان

### 🔒 النسخ المحلية
- جميع النسخ محفوظة محلياً
- لا يتم رفع أي شيء للسحابة

### 🔐 معلومات النسخة
كل نسخة تحتوي على ملف `backup-info.json` يحتوي على:
```json
{
  "BackupName": "backup_20251203_140530",
  "BackupType": "full",
  "Timestamp": "2025-12-03 14:05:30",
  "ProjectPath": "D:\\joker\\workspace\\shadcn-ui",
  "BackupPath": "D:\\joker\\backups\\backup_20251203_140530",
  "IncludedNodeModules": false,
  "ComputerName": "YOUR-PC",
  "UserName": "YourName"
}
```

---

## 🚀 نصائح للاستخدام الأمثل

1. **نسخ دورية**: استخدم `backup-auto.ps1` للنسخ التلقائي
2. **قبل التحديثات الكبيرة**: نسخة يدوية مع `-AutoClean:$false`
3. **اختبار الاستعادة**: اختبر استعادة نسخة في مجلد test
4. **السحابة**: ارفع مجلد backups للسحابة (Google Drive, OneDrive)
5. **Git**: النسخ الاحتياطية تكمل Git ولا تستبدله

---

## 🆘 استكشاف الأخطاء

### ❌ "Cannot find path"
تأكد من وجود المسارات في السكريبت

### ❌ "Access denied"
شغل PowerShell كـ Administrator

### ❌ "Execution Policy"
```powershell
Set-ExecutionPolicy -ExecutionPolicy Bypass -Scope Process
```

### ❌ "Out of disk space"
احذف نسخ قديمة أو غير الوجهة

---

## 📞 الدعم

للمساعدة أو الاستفسارات، راجع:
- سجل العمليات: `D:\joker\backups\backup-log.txt`
- معلومات النسخة: `backup-info.json` داخل كل نسخة

---

## 🎉 الخلاصة

الآن لديك نظام نسخ احتياطي متكامل يحمي مشروعك! 🔒

**للبدء:**
```batch
# نسخة سريعة
backup-quick.bat

# أو PowerShell
.\backup-system.ps1
```

**للاستعادة:**
```batch
# استعادة سريعة
backup-restore.bat

# أو PowerShell
.\backup-restore.ps1
```

---

✅ **نظامك الآن محمي بالكامل!**
