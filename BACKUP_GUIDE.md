# 🔒 نظام النسخ الاحتياطي - Backup System

نظام بسيط وفعّال للنسخ الاحتياطي واستعادة المشروع.

---

## 📁 الملفات

### 1. **backup.bat** - النسخ الاحتياطي
نسخ احتياطي شامل للمشروع.

#### الاستخدام:
```batch
# انقر نقراً مزدوجاً
backup.bat
```

#### ما يتم نسخه:
✅ مجلد `src/` - الكود المصدري
✅ مجلد `public/` - الملفات العامة
✅ مجلد `docs/` - الوثائق
✅ `package.json`
✅ `tsconfig.json`
✅ `vite.config.ts`
✅ `tailwind.config.ts`
✅ `index.html`
✅ جميع ملفات `.bat` و `.ps1`

#### ما لا يتم نسخه (توفير مساحة):
❌ `node_modules/` (يمكن تثبيته بـ `npm install`)
❌ `dist/` و `build/` (ملفات البناء)
❌ `.git/` (موجود على GitHub)
❌ `.cache/` و `temp/`

---

### 2. **restore.bat** - الاستعادة
استعادة نسخة احتياطية سابقة.

#### الاستخدام:
```batch
# انقر نقراً مزدوجاً
restore.bat

# اختر رقم النسخة من القائمة
```

---

### 3. **auto-backup.bat** - النسخ التلقائي
نسخ احتياطي تلقائي كل 6 ساعات.

#### الاستخدام:
```batch
# انقر نقراً مزدوجاً
auto-backup.bat

# سيعمل في الخلفية
# اضغط Ctrl+C للإيقاف
```

---

## 📊 هيكل النسخ الاحتياطية

```
D:\joker\backups\
├── backup_20251203_140530\
│   ├── src\
│   ├── public\
│   ├── docs\
│   ├── package.json
│   ├── backup-info.json
│   └── ...
│
├── backup_20251203_140530.zip  (إذا اخترت الضغط)
│
└── backup_20251203_150245\
    └── ...
```

---

## 🎯 السيناريوهات الشائعة

### 🔹 نسخة قبل تحديث مهم
```batch
backup.bat
# اختر "Y" للضغط
```

### 🔹 نسخة يومية تلقائية
```batch
auto-backup.bat
# دعه يعمل في الخلفية
```

### 🔹 استعادة نسخة بعد مشكلة
```batch
restore.bat
# اختر النسخة المطلوبة
```

---

## 📋 معلومات النسخة

كل نسخة تحتوي على ملف `backup-info.json`:
```json
{
  "backupName": "backup_20251203_140530",
  "timestamp": "03/12/2025 14:05:30",
  "source": "D:\\joker\\workspace\\shadcn-ui",
  "destination": "D:\\joker\\backups\\backup_20251203_140530"
}
```

---

## ⚙️ الإعدادات

يمكنك تعديل:
- **مكان النسخ**: غيّر `DEST` في `backup.bat`
- **الفترة الزمنية**: غيّر `INTERVAL` في `auto-backup.bat`

---

## 💡 نصائح

1. **النسخ الدورية**: شغّل `auto-backup.bat` دائماً
2. **قبل التحديثات**: نسخة يدوية بـ `backup.bat`
3. **اختبار الاستعادة**: جرّب الاستعادة في مجلد test
4. **السحابة**: ارفع مجلد `backups` لـ Google Drive
5. **بعد الاستعادة**: شغّل `npm install`

---

## 🆘 حل المشاكل

### ❌ "لا يوجد مجلد نسخ احتياطية"
- شغّل `backup.bat` أولاً لإنشاء نسخة

### ❌ "فشل النسخ"
- تأكد من وجود مساحة كافية في القرص
- شغّل الملف كـ Administrator

### ❌ "بعد الاستعادة المشروع لا يعمل"
```batch
cd D:\joker\workspace\shadcn-ui
npm install
npm run dev
```

---

## 📊 الحجم المتوقع

| نوع النسخة | الحجم التقريبي |
|------------|----------------|
| غير مضغوطة | ~50-100 MB |
| مضغوطة | ~10-20 MB |

---

## 🎉 البداية السريعة

```batch
# 1. نسخة احتياطية الآن
backup.bat

# 2. تفعيل النسخ التلقائي
auto-backup.bat

# 3. لاستعادة نسخة
restore.bat
```

---

✅ **نظامك الآن محمي!**

نسخ احتياطية منتظمة = راحة بال 😊
